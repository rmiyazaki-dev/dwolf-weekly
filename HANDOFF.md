# D-WOLF週次管理アプリ — 引き継ぎメモ (HANDOFF)

Claude Codeで作業を再開する際は、このファイルを最初に読んでください。

## 概要
- 清水組ドローン事業部(D-WOLF)向けの週次業務管理アプリ
- フロントエンドは単一HTMLファイル(Vanilla JS)+ Supabase(DB・Storage)+ Vercelホスティング
- v3.3でMoney Forward連携用に**初のバックエンド**(Vercel Serverless Functions、`api/`・`lib/`配下)を追加。フロントエンド(index.html)自体は引き続きバックエンドなしの構成
- 本番URL: https://dwolf-weekly-app.vercel.app
- リポジトリ: https://github.com/rmiyazaki-dev/dwolf-weekly (main ブランチ)
- 実体ファイル: `index.html`(画面本体)、`api/mf/*.js`・`lib/mf/*.js`(Money Forward連携のサーバーレス関数)、`vercel.json`(Cron設定)、`package.json`(Node実行環境)

## デプロイフロー
```
ローカルで index.html を編集
→ git add . && git commit -m "..." && git push origin main
→ GitHub連携によりVercelが自動デプロイ(1〜2分)
→ ブラウザで Cmd+Shift+R (強制リロード)して確認
```
robots.txt も同リポジトリのルートに置いてあり、検索エンジン除外用。

## Supabase構成
- プロジェクトURL・anon keyは index.html 冒頭の `SUPABASE_URL` / `SUPABASE_ANON_KEY` に直書き済み(社内限定ツールのため許容)
- テーブル作成SQLは `setup.sql`(基本) → `setup-v3.sql`(顧客・履歴・ファイル追加分) → `setup-mf.sql`(Money Forward連携分・実行済み) → `setup-crm.sql`(CRM化P1: customersへの列追加・**要実行**)の順
- 主なテーブル:
  - `kv`: 汎用キーバリュー(マスター設定 `master`、共通パスワード `app_password`、`settings`、同期状態 `mf_sync_state` など)
  - `weeks`: 週次データ(week_key=YYYYMMDD、スケジュール・TODO・繁忙度・日報サマリー等をJSONBで1レコードに格納)
  - `cases`: 案件(id=タイムスタンプ、dataカラムにJSONBで詳細。`mfQuoteId`/`mfBillingId`/`mfLinkedAt`はMF連携で自動付与)
  - `customers`: 顧客マスタ(v3.5でCRM化: `mf_partner_id`(MF取引先ID・部分UNIQUE)/`name_kana`/`email`/`address`/`updated_at`列を追加。MFの取引先が正マスタとして毎同期で反映される)
  - `case_history`: 案件の変更履歴タイムライン
  - `case_files`: Supabase Storage(`case-files`バケット、公開・50MB上限)のファイル台帳
  - `comments`: 案件コメント
  - `announcements`: 連絡事項(既読管理はreadsカラムのJSONB配列)
  - `audit_log`: 全体の更新履歴
  - `nippo`: 日報(Teamsから自動取込 or 手動送信、teams_msg_idで重複防止)
  - `mf_tokens`(v3.3で追加): MFのOAuthトークン保管専用。**RLS有効・anon/authenticatedへのポリシーなし**。service_role keyのみアクセス可(index.htmlの通常運用とは別扱い)
  - `mf_review_queue`(v3.3で追加): MFの見積書/請求書のうち自動突合できなかったものの一覧。他テーブル同様anon keyで読み書き

## 認証まわり
- アプリ自体: 名前選択 + 共通パスワード(初期値 `dwolf2026`、マスター管理タブから変更可能。試用段階のため簡易方式のまま継続する方針)
- Teams連携: Entra ID(Azure AD)にSPAアプリ登録済み(`DWOLF-Nippo-Reader`)。MSAL.js使用。権限: ChannelMessage.Read.All, Team.ReadBasic.All, Channel.ReadBasic.All, Calendars.Read
  - リダイレクトURIは固定値 `https://dwolf-weekly-app.vercel.app`(location.origin動的生成だと稀にAADSTS500111エラーが出たため固定化した経緯あり)
- Googleカレンダー連携: Google Identity Services使用。個人Gmailアカウント前提でOAuth同意画面は「外部」+テストユーザー登録方式

## 実装済み機能(v3.2時点)
1. 週間ボード: スケジュール入力、TODO、Outlook/Googleカレンダー自動取込、ICS手動取込
2. 案件管理: 顧客・見積額・受注額・請求状況・実施日・納期・**入金予定日**・**次回アクション+期日(超過で上部アラート)**・**失注(理由付き・「失注」ボタンから)**・コメント・ファイル添付・変更履歴。備考は複数行入力可。**担当(全体)とは別に、営業担当・フライト担当を任意で設定可能**(`salesOwner`/`flightOwner`)。**「来週の予定」を「今週の予定」に変更し、週(月曜起点)ごとに履歴として蓄積する方式に変更**(`weeklyPlans:[{weekKey,enteredAt,author,text}]`)。同じ週内の再入力は上書き、週が変わると新しいエントリとして追加され、モーダル内に入力日・氏名付きで過去分が新しい順に表示される。旧`nextPlan`(単一テキスト)は編集時に自動で今週分へ引き継がれ、保存時に削除される
3. カンバンボード: ステータス別に案件を可視化、◀▶で移動
4. ダッシュボード: KPI集計(受注額・**見積合計**・未請求額・入金済等)、**期・月次の受注/売上予測**、分類別/担当別グラフ、**営業担当別の受注額集計・フライト担当別の売上合計集計**(未設定は「未分類」にまとめる)、今月実施一覧。**分類別/営業担当別/フライト担当別は受注額(orderAmount>0)が確定している案件のみ集計**(見積のみの案件は含めない。担当別は従来どおり進行中案件全件+人日)。**各グラフの項目名、および期・月次表の「受注件数」の数字をクリックすると案件管理タブへその条件で絞り込んで遷移**(「絞り込み解除」で戻せる。`dashboardCaseFilter={label, predicate}`で任意の絞り込み条件に対応できる設計)
5. 連絡事項: 掲示板+既読管理
6. 日報: Teamsチャネルから自動取込(Adaptive Card対応・メンション/Workflow添付文除去済み)、「会議資料に反映」ボタンで業務内容/相談事項を自動抽出し会議モード・社長報告に連携(手動編集可、再反映しても上書きしない設計)。**アプリから直接送信する機能はv3.4で廃止**(Teamsからの取込のみ)
7. 会議モード: 繁忙度、応援要否、日報サマリー(編集可)。**担当案件の「今週の予定」(案件管理で入力)が各自の欄に自動表示される**(読み取り専用、今週分が入っている案件のみ表示)。**v3.5(P2)で追加**: 「会議準備」ボタン(MF同期+全員分の日報反映を一括実行)、前週の決定事項をフォロー確認用に繰越表示、過去の会議アーカイブ(weeksテーブルの会議メモ・決定事項を直近12週分一覧表示)
8. 社長報告: A4印刷/PDF出力(週間スケジュール・案件・日報まとめ・決定事項)。**v3.5(P2)で「2. 経営数値サマリー」を追加**(今月受注/今月入金予定/うち入金済/請求済・入金待ち/今期受注累計/今期入金済累計。ダッシュボードと同じ計算式)
9. マスター管理: メンバー・分類・ステータス・繁忙度・顧客マスタ・共通パスワード変更。メンバー/分類/ステータス/繁忙度は**ドラッグ&ドロップ(＋▲▼ボタン)で並び替え可能**
10. Money Forwardクラウド請求書 連携(v3.3〜v3.4・本番稼働中): MFの見積書作成→案件と自動突合(できなければ案件管理タブの「要確認リスト」で検索・期間フィルタ付きで手動紐付け、または新規案件として登録)、見積提出/請求書提出で進捗ステータスも自動更新、請求書発行/入金確認→請求状況・入金予定日も自動更新。設定タブに連携カードあり。マスターのステータスに`請求書提出`(報告書作成と完了の間)が自動追加される
11. **顧客タブ(v3.5・CRM化P1)**: MFの取引先(partners API)を同期ごとに顧客マスタへ自動反映(MFが正。`mf_partner_id`一致で更新、正規化名一致の既存顧客にはIDを採用、無ければ新規insert。連絡先系はアプリ側が空の場合のみMFのdepartments[0]から補完)。顧客一覧(検索・並び替え・最終取引180日超の「休眠」チップ)→行クリックで顧客詳細(累計/今期売上・案件数・最終取引日のKPI、プロフィール自動保存編集、年度別売上表、案件履歴、「案件管理で見る」)。タブ上部に**名寄せツール**(customerId未設定の案件を顧客名ごとにまとめ、正規化名一致候補をプリセレクトして一括紐付け or 新規顧客登録)。MFの見積/請求書が案件に紐付く際、`partner_id`から案件の顧客も自動セットされる(自動同期・手動紐付け・新規案件登録すべて)。ダッシュボードに**顧客別売上ランキングTOP10**(未紐付け案件も正規化名一致でマスタ側に合算・クリックで案件絞り込み)

## v3.2で追加した仕様の要点
- **決算期は6月末締め**。会計年度は7月開始〜翌年6月終了で、`FY_START_MONTH = 7` を変えれば決算月を変更できる
- ダッシュボードの「期・月次の受注/売上(予測)」
  - 期はプルダウンで切替。データが存在する期＋今期が自動で選択肢に出る
  - **受注額**の計上月 = 実施日 →(なければ)納期 →(なければ)入金予定日(`orderBaseDate()`)
  - **売上予測(入金予定)** = 入金予定日ベース。うち請求状況が「入金済」のものが実績
  - **見込(見積)** = 進行中かつ受注額未入力で見積額のある案件(受注前パイプライン)
  - 金額集計は完了(アーカイブ)済も含む。進行中のみのKPIは従来どおり上段に表示
  - 受注済なのに入金予定日が未設定の案件は警告バナーで件数・金額を表示(予測から漏れるため)
  - 期別サマリーで全期の受注額・入金予定・入金済を横並び比較
- マスターの並び替えは `master` JSONの配列順を直接入れ替えて`kv`に保存。ステータス順はカンバンの列順、メンバー順は週間ボード/会議モード/社長報告の行順にそのまま反映される
- 案件の`paymentDate`(入金予定日)はDBスキーマ変更不要(`cases.data` JSONBに追加されるだけ)。**SQLの追加実行は不要**

## Money Forwardクラウド請求書 連携(v3.3)
セットアップ(MF側アプリ登録・Vercel環境変数・SQL実行)は完了し、OAuth連携も成功。フィールド名・入金ステータスのコード値は実装当初は未確認だったが、公式APIクライアント実装([wywy-llc/mf-invoice-api](https://github.com/wywy-llc/mf-invoice-api))のソースで確認し修正済み。

### アーキテクチャ
- index.html(ブラウザ)はMFのclient_secretに触れない。`api/mf/*.js`(Vercel Serverless Functions)がOAuth・ポーリング同期を担当する、このプロジェクト初のバックエンド
- `mf_tokens`テーブルはRLS有効・ポリシーなしにしてあり、`SUPABASE_SERVICE_ROLE_KEY`を持つサーバー側コードだけがアクセスできる(refresh_tokenは会社の請求書・入金データに触れるベアラー資格情報のため、他の設定値より一段階強く保護している)
- Webhookは確認できなかったためポーリング方式。VercelがHobbyプランのためCron Jobsは1日1回までの制限があり、`vercel.json`は`0 21 * * *`(UTC。JST 6:00)の日次実行に設定。即時反映したい時は設定タブの「今すぐ同期」ボタン(手動トリガー、追加認証なし。社内限定ツールという既存運用に合わせた設計)

### 業務ルール(ヒアリング済み・実装反映済み)
1. MFの見積書は既存案件(顧客名+金額で突合)と自動マッチング。**一意に決まらない場合は自動で新規案件を作らず「要確認リスト」**(案件管理タブ)に出す
2. MFで見積書が出されたら → 案件の**進捗ステータスを「見積提出」**に自動更新
3. MFで請求書が出されたら → 進捗ステータスを**「請求書提出」**(マスターの`報告書作成`と`完了`の間に追加済み)に、**請求状況(billingStatus)は「請求済」**に自動更新(既に入金済の場合は入金済のまま)
4. MFで入金確認 → **billingStatusを「入金済」**に自動更新
5. **入金予定日(paymentDate)= 請求書のお支払期限(due_date)** を自動で反映(以前は変更しない方針だったが、実データを見て自動反映に変更)
6. **同じ取引先で見積が複数回出された場合、発行日が新しい方で上書き**する(`quote_date`を比較。再発行・改訂に対応)
7. **見積→請求書は1つの案件に集約する**。MFのAPIには見積書と請求書を直接つなぐフィールドが無いため、「既に見積が紐付いていて・まだ請求書は紐付いておらず・取引先名が一致する案件」を金額の一致に関わらず優先候補にすることで、見積と請求書が別々の案件・別々の要確認リスト項目に「重複」するのを防いでいる(`findQuoteLinkedCandidates`)
8. **見積額(quoteAmount)=見積書の金額、受注額(orderAmount)=請求書の金額(税込)** として別フィールドで保持する(金額が違っても両方登録する)。自動突合・手動紐付けのどちらでも書き込まれる
9. 要確認リストで「見送り」した項目も、**「見送り済みも表示」トグルで再度呼び出して紐付け直せる**(以前は見送ると二度と操作できなかった)。また各項目に「新規案件として登録」ボタンがあり、案件モーダルを取引先名・金額・ステータス・請求状況・入金予定日をプリフィルした状態で開いて、顧客(マスター選択 or 直接入力)も含めてその場で新規登録できる
10. 要確認リストの「紐付ける案件を選択」は、テキスト検索(部分一致・案件名/顧客名/担当名)で絞り込める。また候補は**未完了の案件は常に表示、「完了」済の案件は今期のもの、または前期以前でも直近2ヶ月以内**のものだけに絞られる(`mfCandidateCases()`)
11. **案件名(MFの件名)が完全一致すれば、取引先名・金額が違っても自動で同じ案件に紐付ける**(`findTitleMatch`)。「請求書を先に新規案件として登録し、後から見積が来る」場合(見積→請求書の逆順)にも対応。この際、既に請求書が紐付いている(=請求書提出まで進んでいる)案件のステータスを見積紐付けで「見積提出」に巻き戻さないようガードしている。要確認リストで既にpendingのまま残っていた項目も、後から件名一致で自動突合できた時点で自動的にlinked扱いになる(`resolveReviewQueueItem`)。手動の「新規案件として登録」ボタンでも同名の既存案件があれば確認ダイアログで紐付けを提案する
### ファイル構成
- `lib/mf/fieldMap.js`: MFのレスポンスJSONフィールド名を吸収する層(`extractQuoteFields`/`extractBillingFields`/`normalizePartnerName`/`isPaid`/`amountsClose`)。フィールド名・`payment_status`のコード値(0=未設定 1=未入金 **2=入金済** 3=未払い 4=振込済)は公式クライアント実装で確認済み
- `lib/mf/mfClient.js`: MFのOAuth(認可URL生成・トークン交換・リフレッシュ)、quotes/billings一覧のページング取得。一覧APIは`from`/`to`(期間)が必須パラメータ、レスポンスは`{data:[...], pagination:{...}}`形式。`range_key=updated_at`で直近60日以内に更新があったもの(新規作成・入金確認による変更含む)を取得
- `lib/mf/supabaseAdmin.js`: `mf_tokens`専用、service_role key使用
- `lib/mf/supabaseAnon.js`: `cases`/`case_history`/`audit_log`/`mf_review_queue`/`kv`用、anon key使用
- `api/mf/authorize.js` → `callback.js` → `status.js`: OAuth連携フロー一式
- `api/mf/sync.js`: 同期本体(突合・要確認キュー登録・billingStatus更新)。`Authorization: Bearer $CRON_SECRET`(Cron用)または`?manual=1`(手動ボタン用)で起動。請求書とMF見積書の直接の紐付けフィールドはAPI上存在しないため、billingも取引先名+金額でcaseと突合する(quote経由の紐付けは行わない)
- `setup-mf.sql`: `mf_tokens`・`mf_review_queue`のCREATE TABLE(Supabaseで実行済み)

### セットアップ時のハマりどころ(次回同様の作業をする時のために記録)
- MFの「事業者を選択」画面で対象の事業者に「アプリ連携権限が必要です」と出た場合 → **アプリポータル→「ユーザー」→自分の名前をダブルクリック→編集→「アプリ連携」でクラウド請求書にチェック→保存**が必要(参考: [「権限」の設定方法](https://biz.moneyforward.com/support/app-portal/guide/g008.html))
- **最重要の落とし穴**: MFの1アカウントで複数事業者(このケースでは「株式会社イシン」「有限会社清水組」)にアクセスできる場合、**クラウド請求書の実データはどちらか一方の事業者にしか紐付いていない**。帳票の送付元表示(会社名・住所)だけを見て「有限会社清水組」だと思い込み、OAuth連携でもそちらを選んでいたが、実際にクラウド請求書のデータが入っているのは「株式会社イシン」(事業者番号2991-4449)側だった。`/office`ですら404「非存在ID」になる場合は、まずクラウド請求書の**歯車→事業者設定**で実際の事業者番号を確認し、OAuth連携時に同じ事業者を選んでいるか照合すること
- MFのOAuthスコープは`mfc/invoice/data.write mfc/invoice/data.read`(両方、スペース区切り)が必要。`data.read`単体だと一覧APIが失敗する
- MFの一覧API(`/quotes`, `/billings`)は`from`/`to`(期間)・`q`(検索文字列、空文字でも必須)が必須パラメータ。省略すると404「非存在ID」という紛らわしいエラーになる。`Accept: application/json`ヘッダーも公式クライアントに合わせて付与している
- **実データで判明した非公式クライアント実装との相違点**(公式ドキュメント・非公式クライアントの型定義を信用しすぎないこと):
  - `payment_status`はGETレスポンスでは**数値コードではなく日本語文字列**(`"未入金"`/`"入金済み"`等)で返る。数値コード('0'〜'4')はPUT更新時の送信値専用。`lib/mf/fieldMap.js`の`isPaid()`は文字列判定を主、数値コードは念のためのフォールバックにしている
  - `due_date`/`billing_date`/`sales_date`等の日付フィールドは`"2026/07/21"`(スラッシュ区切り)形式で返る。`created_at`/`updated_at`は`"2026-06-11 11:38:13 +0900"`とまた別形式。`<input type="date">`はISO形式(`YYYY-MM-DD`)必須のため、`toISODate()`(fieldMap.js)・`mfNormDate()`(index.html)で正規化している
- VercelはHobbyプランのため、Cron Jobsは1日1回まで(1時間毎などにすると弾かれる)
- Supabaseの新しいAPIキー体系(`sb_secret_...`)は、従来の`service_role`キー(JWT形式)と同じ用途で使える
- Vercelの環境変数はダッシュボードで追加しただけでは既存デプロイに反映されない。**追加・変更のたびにRedeployが必要**

### 検証状況
- 本番での実データ同期(見積・請求書の自動突合、billingStatus/quoteAmount/orderAmountの自動更新)を確認済み
- `lib/mf/fieldMap.js`の正規化ロジック、`api/mf/sync.js`の突合(見積優先突合・請求書優先突合の両方)・要確認キュー登録・金額/billingStatus更新ロジックは、公式APIクライアント実装で確認したレスポンス形式でモックしたNode統合テストで確認済み
- index.htmlの要確認リストUI(紐付け/見送り/見送り取消/新規案件登録)・設定タブの連携カードは、ローカルSupabaseスタブでのブラウザ実機テストで動作確認済み

## CRM化ロードマップ(2026-08-20にユーザーと合意。全フェーズ完成後にメンバーへローンチ予定)
- **P1: 顧客基盤** — MF取引先同期・名寄せ・顧客タブ・顧客別集計 → **v3.5で実装済み(setup-crm.sqlの実行が必要)**
- **P2: 会議自動化** — 会議準備ボタン・前週決定事項の繰越・社長報告の経営サマリー・会議アーカイブ → **v3.5で実装済み**
- **P3: 受注前管理** — 失注(理由付き)・成約率・失注理由分布・次回アクション+期日アラート → **v3.5で実装済み**。案件に`lost`/`lostReason`/`lostAt`/`nextAction`/`nextActionDate`フィールドを追加(JSONBのためSQL不要)。失注は案件カードの「失注」ボタン(受注額のない案件のみ表示)→理由選択モーダル→アーカイブ。「戻す」で失注も取り消される。ダッシュボードに「受注前パイプライン」カード(成約率=受注/(受注+失注)・商談中・失注額・失注理由の内訳、理由クリックで失注案件を絞り込み表示)。次回アクションは期日超過/当日で案件管理上部にアラート、カードにもチップ表示
- **P4: 更新講習リマインド** — 受講者(個人顧客)ごとの講習受講日を記録し、資格更新時期が近い人のリストを自動生成(請求データ上、更新講習の売上が多く投資対効果が高い)。P1の顧客基盤が前提
- セキュリティ注記: 顧客情報の蓄積が進むため、P1以降のどこかで顧客テーブル周りの保護水準(現状RLSなし・共通パスワード)を見直すことをユーザーに提案済み

## 既知の未対応・今後の検討事項
- セキュリティは共通パスワード方式のまま(Supabase Authへの切替は見送り中、必要になれば再検討)
- ファイルストレージは公開バケット(URL推測困難だが非公開ではない)。マイナンバー等の機微情報は上げない運用でカバー
- 「成清沙織」等、マスター未登録のメンバーが日報を出すと日報タブには出るが、スケジュール/案件/会議モードには反映されない(マスター管理で追加が必要)
- 日報の「業務内容」抽出は正規表現ベース(`業務内容：`〜`明日の予定`手前)。Teams側フォームの項目順が変わると抽出がずれる可能性あり

## 参考ドキュメント(すでにユーザーに渡し済み)
- `Googleカレンダー自動連携_設定手順書.pdf`(管理者向け)
- メンバー向け利用ガイドPDF(作成途中で本セッション終了。必要なら再開)

## 開発者(ユーザー)の環境・好み
- Mac、ターミナル操作は指示があれば実行できるレベル
- コマンドは1つずつ、エラー時は次の一手を具体的に提示する進め方を好む
- 資料はWord/PowerPoint/Excel/PDF納品を好み、Noto Sans JPフォント推奨
- 実装の詳細を削らず、細かい手順を維持したまま資料化してほしいとの要望あり
