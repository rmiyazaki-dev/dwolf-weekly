# HANDOFF — D-BORD 引き継ぎメモ

作業ルールは [AGENTS.md](AGENTS.md)(Codex / Claude Code 共通)。こちらは**機能・仕様・
過去の経緯**の記録。最終更新: v3.17 時点。

## 目次
1. [概要](#1-概要)
2. [Supabase構成](#2-supabase構成)
3. [認証・権限](#3-認証権限)
4. [機能ごとの仕様](#4-機能ごとの仕様)
5. [Money Forward連携](#5-money-forward連携)
6. [外部連携(カレンダー・Teams)](#6-外部連携カレンダーteams)
7. [ハマりどころ集](#7-ハマりどころ集)
8. [未対応・今後の検討事項](#8-未対応今後の検討事項)
9. [ユーザーについて](#9-ユーザーについて)

---

## 1. 概要

清水組ドローン事業部(**D-WOLF**)向けの週次業務管理アプリ。**システム名は D-BORD**。

- D-WOLF = 事業ブランド名、D-BORD = このシステムの名前。ロゴ画像・社長報告書の見出し・
  Teams日報の書式(`【D-WOLF工事日誌_氏名】`)は事業ブランドなので **D-WOLF のまま**
- フロント: 単一HTML(Vanilla JS)+ Supabase(DB/Storage)+ Vercel
- バックエンド: MF連携のためだけに `api/mf/*.js`(Vercel Serverless Functions)がある

### デザインシステム(v3.17)

システムロゴと画面デザインを D-BORD のブランドイメージへ統一。機能・データ構造は変更していない。

- Primary `#0D1B2A` / Secondary `#1B263B`: ヘッダー、ナビ、表見出し
- Accent `#D4AF37`: ブランド強調、選択中タブ、主要導線
- Action `#2563EB`: 日常的な登録・保存・取込操作
- Success `#16A34A` / Warning `#F59E0B` / Danger `#EF4444`: 状態表示
- Neutral: `#111827` / `#374151` / `#6B7280` / `#D1D5DB` / `#F3F4F6` / `#FFFFFF`
- 画面内の色指定は上記13色だけを基準とし、薄い背景・境界線・影はそのRGB値の透明度違いで
  作る。カテゴリ配色も Gold / Action / Success / Secondary / Gray の循環に限定する
- KPI・グラフの意味は、成功・入金済=`Success`、注意・未請求=`Warning`、失注=`Danger`、
  見積・比較=`Action`、ブランド強調=`Accent` に統一する
- デスクトップは提案デザインに合わせた「上部ヘッダー + 220px左サイドナビ + 業務エリア」。
  980px以下ではサイドナビを上部の横スクロール式タブへ切り替える
- ダッシュボードの先頭は、主要4KPI / 補助3KPI / 最新案件 / 進捗アラートの順で構成し、
  期・月次、失注分析、分類・担当別グラフはその下にまとめる。集計式は従来どおり
- 他の11タブもページ見出し、白い業務カード、濃紺の表見出し、フォーム、アラートの
  余白・角丸・境界線・影を同じルールで表示する
- ログイン画面は縦型ロックアップ `d-bord-logo-login.png`、ヘッダーは横型ロックアップ
  `d-bord-logo-header.png`、ファビコンとホーム画面アイコンは `d-bord-favicon.png` を使う
  (`logo.png` はD-WOLF事業ロゴとして残す)
- 共通部品(カード・表・フォーム・モーダル・アラート)と、案件カード・KPI・カンバンを
  同じ角丸・境界線・影・フォーカス表現に揃える

### 画面(12タブ)
週間ボード / TODO / 案件管理 / 顧客 / カンバン / ダッシュボード / 連絡事項 / 日報 /
会議モード / 社長報告 / マスター管理 / 設定

### 運用の背景
- 週1回(**月曜**)の情報共有会議で使う。会議では「先週の実績」を振り返る
- 決算は**6月末締め**(会計年度 = 7月開始〜翌6月)。`FY_START_MONTH = 7`
- メンバー5名 + カレンダー表示用の共有アカウント2つ + 事務2名

---

## 2. Supabase構成

プロジェクトURL・anon keyは `index.html` 冒頭に直書き。**RLSが全テーブルで有効なので
anon keyだけではデータに触れない**(v3.12以降)。

### SQLファイルと実行状況

| ファイル | 内容 | 状況 |
|---|---|---|
| `setup.sql` / `setup-v3.sql` | 基本テーブル・顧客/履歴/ファイル | 実行済み(リポジトリには無し) |
| `setup-mf.sql` | `mf_tokens` / `mf_review_queue` | 実行済み |
| `setup-crm.sql` | `customers` にCRM用の列を追加 | 実行済み |
| `setup-crm2.sql` | `customers.merged_into`(顧客統合用) | 実行済み |
| `setup-todos.sql` | `todos` テーブル | 実行済み |
| `setup-auth.sql` | **全テーブルRLS有効化・ログイン必須化**・Storage非公開化・表示名設定 | 実行済み |
| `setup-viewer-accounts.sql` | 閲覧専用ロールの仕組み(ポリシーを4本に分割) | 実行済み |
| `remove-viewer-role.sql` | 山下・成清のviewerロール解除 | 実行済み |
| `rename-member.sql` | メンバー改名時の一括置換(汎用) | 必要時に使う |

### テーブル

| テーブル | 内容 |
|---|---|
| `kv` | 汎用KV。`master`(マスター設定)・`settings`・`mf_sync_state` |
| `weeks` | 週次データ。`week_key`=YYYYMMDD、全員分を1レコードのJSONBに格納 |
| `cases` | 案件。`id`=タイムスタンプ、`data`列にJSONBで詳細 |
| `customers` | 顧客マスタ。MFの取引先が正マスタ |
| `case_history` | 案件の変更履歴 |
| `case_files` | Storage(`case-files`バケット)のファイル台帳。**バケットは非公開** |
| `comments` | 案件コメント |
| `announcements` | 連絡事項。既読は`reads`列のJSONB配列 |
| `audit_log` | 全体の更新履歴 |
| `nippo` | 日報(Teamsから取込) |
| `todos` | 全員共有のTODO |
| `mf_review_queue` | MF自動突合できなかった見積/請求書 |
| `mf_tokens` | MFのOAuthトークン専用。**RLS有効・ポリシー無し**でservice_roleのみ |

---

## 3. 認証・権限

**Supabase Auth(メール+パスワード)**。v3.12で「名前選択+共通パスワード」から移行。

### 仕組み
- 表示名は `auth.users.raw_user_meta_data.display_name` が正。アプリ内の `me` はここから取る
- 全テーブルRLS有効。ポリシーは `authenticated_select` / `_insert` / `_update` / `_delete` の4本
- 閲覧専用は `raw_app_meta_data.role = 'viewer'`。`public.is_viewer()` で判定し、
  **書き込み系ポリシーで拒否**する
- アカウント追加・パスワードリセットはSupabaseダッシュボードで行う(アプリ内では名前だけ管理)

### アカウント一覧

| 氏名 | メール | 区分 |
|---|---|---|
| 宮﨑隆司 | rmiyazaki@d-wolf.jp | `calendarOnly`(予定表の行のみ。案件の担当には出ない) |
| 岩神星音 | iwagami@d-wolf.jp | `members` |
| 飯田匡輝 | iida@d-wolf.jp | `members` |
| 多々良鮎 | tatara@d-wolf.jp | `members` |
| 妙見修志 | myoken@d-wolf.jp | `members` |
| 山下和美 | yamashita@shimizu-gumi.net | 編集可。`master.members`には**未追加**(担当候補に出ない) |
| 成清沙織 | narikiyo@shimizu-gumi.net | 同上 |
| 共通 / コプロスアリーナ | (ログイン不要) | `calendarOnly`。共有カレンダー取込用 |

### 重要な注意点
- **ログインアカウントとマスターの担当名は「表示名の文字列一致」だけで紐付いている。**
  1文字でも違うと、ログインはできるのに予定表の行・案件の担当・会議モードに出ない
  (**エラーは出ない**ので気づきにくい)
- 正しい氏名表記は **岩神星音**(「岩上」は誤り。v3.12で統一済み)
- 氏名は各所に文字列として保存されるため、改名時は `rename-member.sql` を使う
- 閲覧専用アカウントはログイン直後の**旧データ自動移行をスキップ**する
  (書き込み権限が無く、そのままだとログイン自体が失敗するため)

---

## 4. 機能ごとの仕様

### 4-1. 週間ボード
- メンバー+共有アカウント(`boardRows()` = `members` + `calendarOnly`)の月〜金の予定表
- 下部に**未確認の連絡事項**を表示。チェックで非表示になる
  - 非表示の管理は既読(`announcements.reads`)とは**別**で `settings.annHidden[氏名]`
  - 理由: 投稿時に投稿者自身が既読になる仕様のため、既読を流用すると
    **自分の投稿が一度も表示されない**不具合が起きた(v3.10で分離)

### 4-2. 案件管理
中心となる機能。`cases.data` にJSONBで全項目を持つ(スキーマ変更不要)。

**終了状態はすべて進捗ステータスで表す**(v3.11で `archived` を廃止):
- 「完了」ボタン → ステータスを `完了` に
- 「失注」ボタン → 理由を選んでステータスを `失注` に
- 終了前のステータスは `prevStatus` に保持 → 「進行中に戻す」で復帰
- **消したいときは「削除」**(完全削除・復元不可。案件名の入力による確認あり)
- 判定は `isOpenCase()` / `isDoneCase()` / `isLostCase()` / `isClosedCase()`

**担当体系**(v3.6で変更。旧 `owner` は廃止):
- 営業担当 `salesOwner` … 案件の表記上の担当。受注額の集計単位
- フライト主担当 `flightOwner` / 副担当① `flightOwner2` / 副担当② `flightOwner3`
- 各フライト担当に**比率(%)** `flightRatio1/2/3`。氏名が入っている役の合計が
  100%でないと保存できない
- 担当での絞り込みは営業担当・フライト担当を**横断**して検索する

**日付**: 見積日 `quoteDate` / 受注日 `orderDate` / 実施日 `execDate` / 入金予定日 `paymentDate`
(v3.6で「実施日・納期」から「受注日・実施日」に変更。納期は廃止)

**その他**: 次回アクション+期日(超過で上部にアラート)、今週の予定(`weeklyPlans` に
週ごとの履歴として蓄積)、コメント、ファイル添付、変更履歴

### 4-3. カンバン
- 全案件をステータス列に表示(`完了`列・`失注`列も含む)
- **ドラッグ&ドロップで列移動できる**(v3.16)。◀▶ボタンも併用可
  - ドロップ先が「完了」「失注」の場合は `closeCase()` / `openLostModal()` を経由するので、
    `prevStatus` の保持・失注理由の入力が省略されない
  - 完了・失注済のカードはドラッグ不可。「戻す」ボタンのみ
  - 列のハイライトは `dragleave` のバブリング誤発火を避けるため、`dragover` のたびに
    「今カーソルが乗っている列だけ」付け替える

### 4-4. ダッシュボード
- 上段KPI、期・月次の受注/売上予測、3つのグラフ、顧客別売上ランキングTOP10、今月の実施案件
- **3グラフ(分類別 / 営業担当別 受注額 / フライト担当別 売上)は「期別サマリーの受注件数」と
  必ず一致する範囲で集計する**(受注額確定 + 受注日が選択中の期。完了済も含む)
  - v3.10で修正。以前は「未アーカイブのみ・期の絞り込み無し」で、期別サマリーの21件に対し
    グラフが7件、のように食い違っていた
- フライト担当別の売上は**比率に応じて按分**(比率未入力なら担当人数で均等割り)
- 集計の基準日:
  - 受注額 → **受注日**(`orderBaseDate()` = `orderDate`)
  - 入金予定 → **入金予定日**
  - どちらも空の案件は7月の上の「**日付未指定**」行に集計(期合計には含めない)
- **表示件数とクリック後の一覧は必ず一致する**。集計に使った条件をそのまま行の
  predicate として登録する `barPredicates` 方式(v3.6)

### 4-5. TODO
- 全員共有(`todos` テーブル)。担当・期限・案件の紐付けを持つ
- **TODOタブと会議モードの両方から**追加・完了・編集・削除できる(編集は共通の `todoModal`)
- タブのバッジ = 自分が担当で期限切れ・本日期限の件数

### 4-6. 顧客
- MFの取引先(partners API)を同期ごとに顧客マスタへ自動反映(**MFが正**)
- 顧客詳細: 累計/今期売上・案件数・最終取引日、年度別売上表、案件履歴
  (**行クリックで案件の編集画面が開く**)
- **重複顧客の統合**: MF側で重複登録された取引先をアプリ側でだけ統合。統合元の行は削除せず
  `merged_into` を付けて非表示化(削除するとMF同期で再作成されるため)
- **名寄せツール**: `customerId` 未設定の案件を顧客名ごとにまとめて一括紐付け
- 担当者(`contact_person`)は**1行1名の複数行形式**

### 4-7. 日報 → 会議モード → 社長報告
- 日報はTeamsチャネルから自動取込(アプリから送信する機能は v3.4 で廃止)
- 「会議資料に反映」で業務内容/相談事項を抽出 → 会議モード・社長報告へ
  - **会議は月曜開催のため、集計対象は「先週(前の月〜金)」**(v3.5で変更)
- 会議モードには各自の①今週の予定(カレンダーより)②未完了TODO③担当案件の今週の予定 を転記
- 社長報告(A4印刷/PDF)の「主な案件」の並び順:
  ①今週動きがあった案件 ②進捗ステータスが進んでいる順 ③更新が新しい順

### 4-8. マスター管理
- **メンバーは2種類**(v3.12):
  - `master.members` … 案件の担当になる人(ログインする人)
  - `master.calendarOnly` … 予定表の行だけ持つ名前(社長・共有カレンダー)。
    案件の担当・会議モード・日報集計には出ない
- メンバー/分類/ステータス/繁忙度はドラッグ&ドロップ(+▲▼)で並び替え可能。
  ステータス順 = カンバンの列順、メンバー順 = 各画面の行順にそのまま反映される

---

## 5. Money Forward連携

**本番稼働中。** 見積書・請求書・取引先を毎日自動で同期する。

### アーキテクチャ
- ブラウザはMFの `client_secret` に触れない。`api/mf/*.js` がOAuth・同期を担当
- Webhookが無いためポーリング方式。VercelがHobbyプランで**Cronは1日1回まで**のため
  `vercel.json` は `0 21 * * *`(UTC = JST 6:00)。即時反映は設定タブの「今すぐ同期」

### ファイル構成
| ファイル | 役割 |
|---|---|
| `lib/mf/fieldMap.js` | MFレスポンスのフィールド名を吸収。`isPaid()` / `normalizePartnerName()` など |
| `lib/mf/mfClient.js` | OAuth・一覧取得。`range_key=updated_at` で直近60日に**更新があった**ものを取得 |
| `lib/mf/supabaseAdmin.js` | `mf_tokens` 専用(service_role) |
| `lib/mf/supabaseAnon.js` | 案件等のDBアクセス。**service_role key を使う**(RLSがあるためanonでは弾かれる) |
| `api/mf/authorize.js` → `callback.js` → `status.js` | OAuth連携フロー |
| `api/mf/sync.js` | 同期本体。`Bearer $CRON_SECRET`(Cron) or `?manual=1`(手動) |

### 業務ルール
1. 見積書 → 既存案件と自動突合。**一意に決まらなければ自動で新規案件を作らず「要確認リスト」へ**
2. 見積書が出たら進捗ステータスを「見積提出」に
3. 請求書が出たら「請求書提出」+ 請求状況「請求済」+ 入金予定日 = 請求書の支払期限
4. **入金確認されたら請求状況を「入金済」に**。この確認は**紐付け済みの案件も毎回行う**ので、
   あとから入金が確定した場合も次回同期で反映される
5. **紐付け済みの見積がMF側で金額改定された場合も反映する**(v3.14)。
   以前は `if (existing) continue` で完全にスキップされ、金額が古いままだった
6. **類似案件を複製して件名だけ変えた場合、案件名もMFの新しい件名に追随する**(v3.16)。
   以前は金額だけ更新され案件名が古いまま残った
7. 同じ取引先で見積が複数回出た場合は**発行日が新しい方で上書き**
8. **見積→請求書は1つの案件に集約する。** MFのAPIには見積書と請求書を直接つなぐフィールドが
   無いため、「既に見積が紐付いていて・まだ請求書が紐付いておらず・取引先名が一致する案件」を
   金額に関わらず優先候補にする(`findQuoteLinkedCandidates`)
9. **案件名(MFの件名)が完全一致すれば、取引先名・金額が違っても自動で同じ案件に紐付ける**
   (`findTitleMatch`)。請求書が先で見積が後、という逆順にも対応
10. 突合の優先順位: ①件名の完全一致 → ②見積紐付け済み・同一取引先 → ③取引先名+金額
11. 要確認リストは検索(部分一致)で絞り込み可。「見送り」した項目も
    「見送り済みも表示」で呼び戻せる。「新規案件として登録」でプリフィル済みモーダルが開く

---

## 6. 外部連携(カレンダー・Teams)

### Googleカレンダー / Outlook
- **取込範囲は今週から5週間(約1ヶ月先)**。予定は日付が属する週の `weeks` レコードへ
  書き分ける(`applyCalendarEvents()`)。土日と範囲外は取り込まない
- **1アカウント内の複数カレンダーから選べる**。選択は `settings.calSelect[provider][対象]`
- **取込先を選べる**。自分の行だけでなく、共有カレンダー(`calendarOnly` の行)にも
  メンバーが代わりに取り込める。設定した人を `settings.calOwner[対象]` に記録し、
  自動取込はその人のときだけ動く
- 取り込んだ予定の文言は `week.calImported[氏名]` に記録。「連携リセット」で
  **取込分だけ削除できる(手入力の予定は残る)**
- **毎日の自動取込**: `settings.calAuto[氏名]` がONなら、6時以降にアプリを開いた初回に
  自動実行(1日1回)。**ブラウザアプリなので誰も開いていない時刻には動けない**

### Teams(日報)
- Entra IDにSPAアプリ登録済み(`DWOLF-Nippo-Reader`)。MSAL.js使用
- リダイレクトURIは固定値 `https://dwolf-weekly-app.vercel.app`
  (`location.origin` 動的生成だと稀に AADSTS500111 が出たため固定化)
- 取込対象は本文が `【D-WOLF工事日誌_氏名】` 形式の投稿。氏名がマスターと一致すると人別に紐付く

---

## 7. ハマりどころ集

### MF連携
- **最重要**: MFの1アカウントで複数事業者にアクセスできる場合、**クラウド請求書の実データは
  どちらか一方の事業者にしか紐付いていない**。帳票の送付元表示だけを見て事業者を判断しないこと。
  `/office` ですら404「非存在ID」になる場合は、クラウド請求書の**歯車→事業者設定**で
  実際の事業者番号を確認し、OAuth連携時に同じ事業者を選んでいるか照合する
  (このケースでは「有限会社清水組」ではなく「株式会社イシン」側にデータがあった)
- OAuthスコープは `mfc/invoice/data.write mfc/invoice/data.read` の**両方**が必要
- 一覧API(`/quotes`, `/billings`)は `from`/`to`/`q`(空文字でも)が**必須**。
  省略すると404「非存在ID」という紛らわしいエラーになる
- **公式ドキュメント・非公式クライアントの型定義を信用しすぎないこと**:
  - `payment_status` はGETでは**日本語文字列**(`"入金済み"` 等)で返る。数値コードはPUT専用
  - 日付は `"2026/07/21"`(スラッシュ)形式。`created_at` は `"2026-06-11 11:38:13 +0900"` と別形式。
    `<input type="date">` はISO形式必須なので `toISODate()` / `mfNormDate()` で正規化
- アプリポータルで「アプリ連携権限が必要です」と出たら、ユーザー編集→「アプリ連携」で
  クラウド請求書にチェックが必要

### インフラ
- VercelはHobbyプランのため**Cronは1日1回まで**
- Vercelの環境変数は追加しただけでは既存デプロイに反映されない。**毎回Redeployが必要**
- Supabaseの新APIキー体系(`sb_secret_...`)は従来の `service_role` キーと同用途

### Googleカレンダー
- **OAuthクライアントと同じGoogle Cloudプロジェクトで「Google Calendar API」の有効化が必要。**
  未有効だと403 `accessNotConfigured`。`origin_mismatch`(承認済みJavaScript生成元の登録漏れ)・
  テストユーザー登録とは**別の設定**なので混同しないこと

---

## 8. 未対応・今後の検討事項

- **CRM化ロードマップ**: P1(顧客基盤)・P2(会議自動化)・P3(受注前管理)は実装済み。
  **P4「更新講習リマインド」は未着手** — 受講者ごとの講習受講日を記録し、資格更新時期が
  近い人のリストを自動生成する構想(更新講習の売上が多く投資対効果が高い)。P1の顧客基盤が前提
- 顧客統合(`merged_into`)の**取り消し機能は未実装**。必要ならSupabaseで直接nullに戻す
- 日報の「業務内容」抽出は正規表現ベース(`業務内容：`〜`明日の予定`手前)。
  Teams側フォームの項目順が変わると抽出がずれる
- カレンダーの自動取込は「6時以降の初回起動時」であって、**本当に朝6時に走るわけではない**。
  サーバー側Cronで実行するには各人のリフレッシュトークンをサーバーに保管する仕組みが必要
- メンバー向け操作マニュアル(Word)を作成済み。仕様変更に追随できていない可能性あり

---

## 9. ユーザーについて

- 宮﨑隆司さん(代表取締役)。**非エンジニア**。Mac、ターミナルは指示があれば実行できる
- コマンドは**1つずつ**、エラー時は次の一手を具体的に提示する進め方を好む
- 資料はWord/PowerPoint/Excel/PDF納品を好む(Noto Sans JPフォント推奨)
- 実装の詳細を削らず、細かい手順を維持したまま資料化してほしいとの要望あり
- 全メンバーへのローンチはCRM化フェーズ完成後の予定
